import {
    React
} from 'react';
import {
    View,


} from 'react-native';

class CountNum extends Componet {
    state = {
        count: this.props.time,
    };
    render(){
        const
    }
}
export default class Game extends Componet {
    constructor (props) {
        super(props);
    }
    state
}